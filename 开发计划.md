# CBA Live Insight 开发计划

## 📋 项目概述

**项目名称**: CBA Live Insight (CBA 赛场洞察)  
**项目类型**: Chrome Extension (Manifest V3)  
**技术栈**: React 18 + Vite + Tailwind CSS + React Query  
**开发周期**: 预计 5-7 个工作日

---

## 🎯 开发阶段划分

### 阶段一：项目初始化与环境搭建 (0.5天)

#### 1.1 项目初始化
- [ ] 使用 Vite 创建 React 项目
  ```bash
  npm create vite@latest cba-live-insight -- --template react
  cd cba-live-insight
  npm install
  ```

#### 1.2 安装核心依赖
- [ ] 安装构建工具和插件
  ```bash
  npm install -D @crxjs/vite-plugin tailwindcss postcss autoprefixer
  npx tailwindcss init -p
  ```

- [ ] 安装数据管理库
  ```bash
  npm install @tanstack/react-query
  ```

- [ ] 安装图标库
  ```bash
  npm install lucide-react
  ```

#### 1.3 配置构建工具
- [ ] 配置 `vite.config.js`
  - 引入 `@crxjs/vite-plugin`
  - 配置 manifest.json 路径
  - 配置构建输出目录

- [ ] 配置 `tailwind.config.js`
  - 设置 content 路径
  - 配置自定义颜色（深色主题 + CBA 橙红色 #FF6600）
  - 配置磨砂玻璃效果（backdrop-blur）

#### 1.4 创建基础目录结构
```
/src
  /assets          # 静态资源（图标、图片）
  /components      # React 组件
  /hooks           # 自定义 Hooks
  /utils           # 工具函数
  /content         # Content Script 入口
  /background      # Background Script（如需要）
  /styles          # 全局样式
  manifest.json
  vite.config.js
```

---

### 阶段二：Manifest 配置与基础架构 (0.5天)

#### 2.1 配置 manifest.json
- [ ] 设置基本信息（name, version, description）
- [ ] 配置 permissions: `["storage"]`
- [ ] 配置 host_permissions:
  - `https://live.douyin.com/*`
  - `https://vms-sc.miguvideo.com/*`
  - `https://v0-sc.miguvideo.com/*`
- [ ] 配置 content_scripts:
  - matches: `["https://live.douyin.com/*"]`
  - js: `["src/content/index.jsx"]`
  - css: `["src/content/style.css"]`
  - run_at: `"document_idle"`
- [ ] 配置 web_accessible_resources（如需要）

#### 2.2 创建 Content Script 入口
- [ ] 创建 `src/content/index.jsx`
  - 创建 Shadow DOM 容器（样式隔离）
  - 挂载 React 根组件
  - 处理页面加载时机

#### 2.3 设置样式隔离
- [ ] 创建 `src/content/style.css`
  - 使用 Shadow DOM 或 Tailwind prefix 防止样式污染
  - 配置全局样式重置

---

### 阶段三：数据接口层开发 (1天)

#### 3.1 创建工具函数
- [ ] `src/utils/api.js`
  - 封装 fetch 请求（统一错误处理）
  - 处理跨域请求
  - 添加请求超时处理

- [ ] `src/utils/dataParser.js`
  - 解析比赛列表数据
    - 提取key为 "timeStamp"的值并转换为日期 Key (YYYYMMDD)
    - 从 matchList[todayKey] 获取当日比赛
    - 映射数据格式（mgdbId, title, time, status, teams）
  - 解析统计数据
    - 提取球队统计（teamStatisticList）
    - 提取球员统计（playerStatisticList）
    - 合并 playerInfo 和 playDataList

#### 3.2 创建自定义 Hooks

- [ ] `src/hooks/useMatchList.js`
  - 使用 React Query 管理比赛列表数据
  - 实现手动刷新功能
  - 处理加载状态和错误状态
  - 数据格式：
    ```javascript
    {
      id: match.mgdbId,
      label: `${match.keyword} ${match.pkInfoTitle}`,
      status: match.matchStatus, // "0"=未开始, "2"=进行中/完赛
      homeTeam: { name, logo, score },
      awayTeam: { name, logo, score }
    }
    ```

- [ ] `src/hooks/useLiveStats.js`
  - 接收 matchId 参数
  - 使用 React Query 实现 10 秒轮询
  - 处理数据为空的情况（比赛未开始）
  - 保留上一次成功数据（容错处理）
  - 返回格式化的统计数据：
    ```javascript
    {
      homeTeam: { stats: {...}, players: [...] },
      awayTeam: { stats: {...}, players: [...] }
    }
    ```

---

### 阶段四：UI 组件开发 (2天)

#### 4.1 基础组件

- [ ] `src/components/FloatingBtn.jsx` (收起态)
  - 圆形/胶囊形悬浮按钮
  - 固定在屏幕右侧
  - 显示 CBA Logo 或 "Data" 文字
  - 点击展开面板
  - 使用 Tailwind 实现磨砂玻璃效果

#### 4.2 主面板组件

- [ ] `src/components/MainPanel.jsx` (展开态容器)
  - 宽度约 360px
  - 深色半透明背景 + 磨砂玻璃
  - 可拖拽移动（可选）
  - 收起/展开切换逻辑

- [ ] `src/components/Header.jsx` (顶部控制栏)
  - 下拉选择器（MatchSelect 组件）
  - 刷新按钮（↻）
  - 关闭按钮（×）
  - 布局：左侧下拉框，右侧操作按钮

- [ ] `src/components/MatchSelect.jsx` (比赛选择器)
  - 自定义下拉组件（或使用 Headless UI）
  - 显示格式：`时间 对阵`（如：`12月20日19:35 北京北汽vs深圳马可波罗`）
  - 状态标识（未开始/进行中）
  - 空状态处理（"请选择比赛"）

#### 4.3 数据展示组件

- [ ] `src/components/ScoreBoard.jsx` (记分牌)
  - 布局：`[客队Logo] [比分] : [比分] [主队Logo]`
  - 比分数字动画（变化时绿色/红色跳动）
  - 显示比赛状态（Live / 未开始 / 已结束）
  - 显示节数/剩余时间（如接口提供）

- [ ] `src/components/StatsTabs.jsx` (数据切换 Tab)
  - Tab 1: 球队对比
  - Tab 2: 球员统计
  - 使用 Headless UI Tabs 或自定义实现

- [ ] `src/components/TeamStats.jsx` (球队对比)
  - 使用双向进度条展示数据对比
  - 数据项：
    - 投篮命中率 (fieldGoalsPercentage)
    - 三分命中率 (threePointPercentage)
    - 罚球命中率 (freeThrowsPercentage)
    - 篮板 (rebounds)
    - 助攻 (assists)
    - 失误 (turnovers)
  - 布局：`[45%] <---- 投篮命中率 ----> [52%]`
  - 使用 CBA 橙红色 (#FF6600) 作为强调色

- [ ] `src/components/PlayerBox.jsx` (球员统计)
  - 内嵌子 Tab: [客队] | [主队]
  - 表格列：
    - Player: 头像 + 号码 + 名字
    - PTS: 得分 (points)
    - +/-: 正负值 (plusMinus)
    - Stat: 动态显示（如 "10板" 或 "8助"）
  - 在场状态：在场球员名字前加绿色小圆点 ● (onCourt === "1")
  - 表格排序（按得分、篮板、助攻等）

---

### 阶段五：样式与动效 (1天)

#### 5.1 主题配置
- [ ] 深色模式配色方案
  - 背景：黑色半透明 (rgba(0, 0, 0, 0.8))
  - 文字：白色/浅灰色
  - 强调色：CBA 橙红色 (#FF6600)
  - 边框：半透明白色

- [ ] 磨砂玻璃效果
  - `backdrop-blur-md` 或 `backdrop-blur-lg`
  - 半透明背景
  - 边框高光

#### 5.2 动画效果
- [ ] 面板展开/收起动画（fade + slide）
- [ ] 比分数字变化动画（scale + color transition）
- [ ] 数据加载骨架屏（Skeleton）
- [ ] 按钮 hover 效果
- [ ] 表格行 hover 效果

#### 5.3 响应式处理
- [ ] 确保在不同屏幕尺寸下正常显示
- [ ] 移动端适配（可选，主要针对 PC）

---

### 阶段六：错误处理与优化 (1天)

#### 6.1 错误处理
- [ ] 网络请求失败处理
  - 显示错误提示
  - 保留上一次成功数据
  - 提供重试按钮

- [ ] 数据为空处理
  - 比赛未开始时显示占位图
  - 提示 "比赛未开始" 或 "暂无数据"

- [ ] 接口返回异常处理
  - code !== 200 的情况
  - 数据格式异常

#### 6.2 性能优化
- [ ] 防抖处理（刷新按钮）
- [ ] 数据缓存策略（React Query 配置）
- [ ] 组件懒加载（如需要）
- [ ] 减少不必要的重渲染（React.memo）

#### 6.3 用户体验优化
- [ ] 加载状态提示（Loading Spinner）
- [ ] 空状态占位图
- [ ] 操作反馈（Toast 提示）
- [ ] 记住用户选择的比赛（localStorage）

---

### 阶段七：测试与部署 (0.5-1天)

#### 7.1 功能测试
- [ ] 测试比赛列表加载
- [ ] 测试比赛选择功能
- [ ] 测试统计数据轮询（10秒间隔）
- [ ] 测试数据更新动画
- [ ] 测试错误场景（网络断开、接口异常）
- [ ] 测试样式隔离（不影响抖音页面）

#### 7.2 兼容性测试
- [ ] Chrome 浏览器测试
- [ ] 不同分辨率测试
- [ ] 不同网络环境测试

#### 7.3 构建与打包
- [ ] 运行 `npm run build`
- [ ] 检查 dist 目录结构
- [ ] 验证 manifest.json 配置

#### 7.4 部署说明
- [ ] 编写 README.md（安装和使用说明）
- [ ] 准备插件图标（assets/icon.png）
- [ ] 测试加载插件到 Chrome

---

## 📝 开发注意事项

### 数据接口关键点
1. **接口 A（比赛列表）**：
   - URL: `https://vms-sc.miguvideo.com/vms-match/v6/staticcache/basic/match-list/normal-match-list/0/2221401/default/1/miguvideo`
   - 关键字段：`body.sysdate` (时间戳) → 转换为日期 Key (YYYYMMDD)
   - 关键字段：`body.matchList[todayKey]` → 当日比赛数组
   - 关键字段：`match.mgdbId` → 用于接口 B 的 matchId

2. **接口 B（统计数据）**：
   - URL: `https://v0-sc.miguvideo.com/vms-livedata/live-stream/v1/tech-statistic/{matchId}`
   - 关键字段：`body.statisticInfoList` → 数组，包含主客两队数据
   - 关键字段：`teamStatisticList` → 球队统计数据
   - 关键字段：`playerStatisticList` → 球员统计数据
   - 关键字段：`onCourt === "1"` → 在场球员标识

### 样式隔离方案
- **推荐方案 1**：使用 Shadow DOM（完全隔离）
- **备选方案 2**：使用 Tailwind prefix（如 `cba-` 前缀）

### 数据轮询策略
- 使用 React Query 的 `refetchInterval: 10000` (10秒)
- 当 matchId 为空或比赛未开始时，停止轮询
- 网络错误时，保留上一次成功数据，不显示错误状态

### 状态管理
- 使用 React Query 管理服务器状态（比赛列表、统计数据）
- 使用 React useState 管理 UI 状态（面板展开/收起、Tab 切换）

---

## 🎨 UI 设计规范

### 颜色方案
- **主背景**: `rgba(0, 0, 0, 0.85)` + `backdrop-blur-lg`
- **强调色**: `#FF6600` (CBA 橙红色)
- **文字主色**: `#FFFFFF`
- **文字次色**: `#CCCCCC`
- **边框色**: `rgba(255, 255, 255, 0.1)`
- **成功色**: `#10B981` (绿色，用于在场标识)
- **错误色**: `#EF4444` (红色，用于错误提示)

### 字体大小
- 标题: `text-lg` 或 `text-xl`
- 正文: `text-sm` 或 `text-base`
- 小字: `text-xs`

### 间距规范
- 组件间距: `space-y-4` 或 `gap-4`
- 内边距: `p-4` 或 `px-4 py-3`
- 圆角: `rounded-lg` 或 `rounded-xl`

---

## 📦 依赖清单

### 生产依赖
```json
{
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "@tanstack/react-query": "^5.0.0",
  "lucide-react": "^0.300.0"
}
```

### 开发依赖
```json
{
  "@vitejs/plugin-react": "^4.2.0",
  "@crxjs/vite-plugin": "^2.0.0",
  "vite": "^5.0.0",
  "tailwindcss": "^3.4.0",
  "postcss": "^8.4.0",
  "autoprefixer": "^10.4.0"
}
```

---

## ✅ 验收标准

1. ✅ 插件能在 `https://live.douyin.com/*` 正常加载
2. ✅ 能正确获取并显示当日比赛列表
3. ✅ 选择比赛后能正常轮询统计数据（10秒间隔）
4. ✅ 比分变化时有动画效果
5. ✅ 球队对比数据正确显示（双向进度条）
6. ✅ 球员统计数据正确显示（表格形式）
7. ✅ 在场球员有绿色标识
8. ✅ 样式不影响抖音原页面
9. ✅ 错误场景有友好提示
10. ✅ 界面美观，符合设计规范

---

## 🚀 快速开始

1. 按照阶段一完成项目初始化
2. 按照阶段二配置 manifest.json
3. 按照阶段三开发数据接口层
4. 按照阶段四开发 UI 组件
5. 按照阶段五完善样式和动效
6. 按照阶段六处理错误和优化
7. 按照阶段七测试和部署

---

## 📚 参考资源

- [Chrome Extension Manifest V3 文档](https://developer.chrome.com/docs/extensions/mv3/)
- [React Query 文档](https://tanstack.com/query/latest)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [@crxjs/vite-plugin 文档](https://crxjs.dev/vite-plugin)

---

**最后更新**: 2025-01-XX  
**文档版本**: v1.0

